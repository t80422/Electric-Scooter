@model Electric_Scooter.Models.Supplier

<main class="container-wrap">
    <div class="container-main">
        <!-- breadcrumb -->
        <section class="breadcrumb-wrap">
            <ul>
                <li>
                    <a href="">首頁</a>
                </li>
                <li>
                    <a href="@Url.Action("Index")">供應商管理</a>
                </li>
                <li>
                    <p>修改</p>
                </li>
            </ul>
        </section>
        <!-- main -->
        @using (Html.BeginForm("Edit", "Supplier", FormMethod.Post))
        {
            @Html.HiddenFor(x => x.s_Id)
            @Html.HiddenFor(x => x.s_CreateDate)
            @Html.Partial("Form", Model)
        }
    </div>
</main>